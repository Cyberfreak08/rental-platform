# AI Coding Agent Context

This directory contains instructions for Bob/Codex-style coding agents.

Read `Bob-coding-rules.md` before implementation.

The final master prompt should point the agent at the repository documentation rather than duplicating the entire specification in the prompt.
